import { ChangeDetectorRef, ElementRef } from '@angular/core';
import { FormArray } from '@angular/forms';
import { DragDropConfig } from '../config/drag-drop-config';
import { DragDropSortableService } from '../service/drag-drop-sortable/drag-drop-sortable.service';
import { DragDropService } from '../service/drag-drop/drag-drop.service';
import { AbstractDirective } from './abstract/abstract.directive';
export declare class SortableContainerDirective extends AbstractDirective {
    private sortableDataService;
    private _sortableData;
    private _sortableHandler;
    sortableData: Array<any> | FormArray;
    constructor(elementRef: ElementRef, dragDropService: DragDropService, config: DragDropConfig, cdr: ChangeDetectorRef, sortableDataService: DragDropSortableService);
    dragEnterCallback(event: Event): void;
    getItemAt(index: number): any;
    indexOf(item: any): number;
    removeItemAt(index: number): void;
    insertItemAt(item: any, index: number): void;
}
