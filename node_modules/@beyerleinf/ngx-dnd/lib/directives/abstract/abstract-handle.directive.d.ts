import { ElementRef } from '@angular/core';
import { DragDropConfig } from '../../config/drag-drop-config';
import { DragDropService } from '../../service/drag-drop/drag-drop.service';
import { AbstractDirective } from './abstract.directive';
export declare abstract class AbstractHandleDirective {
    dragDropService: DragDropService;
    config: DragDropConfig;
    private directive;
    element: HTMLElement;
    constructor(elementReference: ElementRef, dragDropService: DragDropService, config: DragDropConfig, directive: AbstractDirective);
}
